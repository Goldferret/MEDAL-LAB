# ROS Configuration for DOFBOT Pro
# 
# This file contains ONLY ROS-specific configuration.
# All MADSci network configuration is in ../../.env.global
#
# The Makefile uses: docker compose --env-file ../../.env.global
# This makes .env.global variables available for both:
#   1. YAML substitution (${VAR} in docker-compose.yml)
#   2. Container environment (via env_file: directive)

# ============================================================================
# ROS ENVIRONMENT VARIABLES
# ============================================================================

# ROS Master URI (assumes ROS master running on host)
ROS_MASTER_URI=http://localhost:11311

# ROS Distribution
ROS_DISTRO=noetic

# Note: ROS_IP is set to empty in docker-compose.yml to allow ROS auto-detection
# With network_mode: host, ROS will automatically use the host's IP address
# This matches DOFBOT_PRO_1_HOST from ../../.env.global (192.168.1.73)


