# MADSci Core Server Local Configuration
# 
# This file contains ONLY configuration specific to running MADSci core services.
# All network addresses, ports, and URLs are defined in ../../.env.global
#
# The Makefile uses: docker compose --env-file ../.env.global
# This makes .env.global variables available for both:
#   1. YAML substitution (${VAR} in docker-compose.yml)
#   2. Container environment (via env_file: directive)

# ============================================================================
# LOCAL-ONLY CONFIGURATION
# ============================================================================
# These variables are NOT in .env.global because they're specific to how
# MADSci core services run, not part of the global network configuration

# Service binding addresses (0.0.0.0 = listen on all interfaces)
EVENT_HOST=0.0.0.0
RESOURCE_HOST=0.0.0.0
DATA_HOST=0.0.0.0
EXPERIMENT_HOST=0.0.0.0
WORKCELL_HOST=0.0.0.0
LOCATION_HOST=0.0.0.0
LAB_HOST=0.0.0.0

