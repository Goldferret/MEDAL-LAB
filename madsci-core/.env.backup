# MADSci Core Server Configuration
# These settings customize the MADSci core server deployment
# Network settings and database ports are inherited from ../../.env.global

# ============================================================================
# DOCKER COMPOSE YAML SUBSTITUTION VARIABLES
# ============================================================================
# MinIO console port (needed for Docker Compose YAML substitution)
MINIO_CONSOLE_PORT=9001

# Port numbers for docker-compose YAML substitution
# IMPORTANT: These MUST match the *_SERVER_PORT values in ../../.env.global
# They exist here because docker-compose needs them for ${VAR} substitution in the YAML
# The actual source of truth is .env.global - keep these in sync!
LAB_SERVER_PORT=8000
EVENT_SERVER_PORT=8001
EXPERIMENT_SERVER_PORT=8002
RESOURCE_SERVER_PORT=8003
DATA_SERVER_PORT=8004
WORKCELL_SERVER_PORT=8005
LOCATION_SERVER_PORT=8006

# ============================================================================
# SERVICE BINDING CONFIGURATION
# ============================================================================
# These configure which network interface each service binds to for listening
# 0.0.0.0 = listen on all interfaces (required for network_mode: host)

# Event Manager
EVENT_HOST=0.0.0.0

# Resource Manager  
RESOURCE_HOST=0.0.0.0

# Data Manager
DATA_HOST=0.0.0.0

# Experiment Manager
EXPERIMENT_HOST=0.0.0.0

# Workcell Manager
WORKCELL_HOST=0.0.0.0

# Location Manager
LOCATION_HOST=0.0.0.0

# Lab Manager
LAB_HOST=0.0.0.0
